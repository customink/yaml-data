name: "units-test"
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'
jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: npm ci
    - run: npm test
  # test action works running
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Exercise
      uses: ./
      id: exercise
      with:
        file: test.yaml
        get: Parameters.XDataName.String
    - name: Assert
      run: |
        value="${{ steps.exercise.outputs.value }}"
        echo "Found value: ${value}"
        if [ "${value}" = "x-data-value" ]; then
          echo "✅"
        else
          echo "🚫"
          exit 1
        fi
