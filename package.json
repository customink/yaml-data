{
  "name": "yaml-data",
  "version": "1.0.0",
  "description": "YAML Data Action",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "dependencies": {
    "@actions/core": "^1.9.1",
    "js-yaml": "^4.1.0",
    "js-yaml-cloudformation-schema": "^1.0.0",
    "lodash.get": "^4.4.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1",
    "eslint": "^8.0.0",
    "jest": "^27.2.5",
    "prettier": "2.7.1"
  }
}
